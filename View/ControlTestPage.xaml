<UserControl x:Class="SoundDesigner.View.ControlTestPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:SoundDesigner.Controls"
        xmlns:viewModel="clr-namespace:SoundDesigner.ViewModel"
        mc:Ignorable="d"
        Background="Transparent"
        Height="510" Width="800">

    <UserControl.Resources>
        <ImageBrush x:Key="PanelBrush" ImageSource="/Assets/background-texture.png"/>
    </UserControl.Resources>


        <Border Background="{StaticResource PanelBrush}" BorderBrush="White" BorderThickness="2" CornerRadius="5" Height="500" Width="600">
            
            <Canvas x:Name="PanelCanvas">
                <controls:AudioJack Canvas.Left="200" Canvas.Top="150" ToY="0" />
            </Canvas>

        </Border>



</UserControl>
