<Window x:Class="SoundDesigner.MainWindowControlTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:SoundDesigner.Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="510" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
        </Grid.RowDefinitions>
        <!--<Canvas>
            <controls:MainPanel Width="Auto" Height="Auto">
            </controls:MainPanel>
        </Canvas>-->

        <controls:AudioJack Grid.Row="0" ToY="0" HorizontalAlignment="Left"/>

        <Canvas Grid.Row="1" HorizontalAlignment="Left">
            <controls:AudioJack Grid.Row="0" ToY="0" Padding="0" Margin="0" Height="20" Width="20" />
        </Canvas>
        
    </Grid>
</Window>
